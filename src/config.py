"""
이 모듈은 어플리케이션의 핵심 설정들을 관리합니다.
환경 변수(API 키), AI 모델 이름, 예상 비용 측정을 위한 단가표, 
그리고 LLM에 전달되는 주 프롬프트 및 검증(Validation) 프롬프트를 정의합니다.
"""

import os
from dotenv import load_dotenv

# API Key 및 Google Drive Folder ID 로드
load_dotenv()

GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY")
if not GEMINI_API_KEY:
    raise ValueError("GEMINI_API_KEY가 설정되지 않았습니다. .env 파일에 추가해주세요.")

DRIVE_ROOT_FOLDER_ID = os.environ.get("DRIVE_ROOT_FOLDER_ID")
if not DRIVE_ROOT_FOLDER_ID:
    raise ValueError("DRIVE_ROOT_FOLDER_ID가 설정되지 않았습니다. .env 파일에 타겟 구글 드라이브 폴더 ID를 추가해주세요.")

# 모델 설정
MODEL_NAME = 'gemini-2.5-pro'

# 요금 단가 설정 (Gemini 1.5/2.5 Pro Pay-as-you-go 기준, 128k 이하)
# 1M tokens 당 USD
INPUT_PRICE_PER_M = 1.25
OUTPUT_PRICE_PER_M = 5.00

# 재시도 설정
MAX_RETRIES = 3
RETRY_DELAY_SECONDS = 60

# 프롬프트 템플릿
ANALYSIS_PROMPT_TEMPLATE = """
다음은 분석해야 할 논문의 전체 (또는 일부) 텍스트입니다:

{paper_text}

---
위 논문에 대해서 다음 8가지 항목에 대한 분석을 진행해서 그 결과를 마크다운으로 작성해줘.
주의사항 1: "다음은 분석 결과입니다"나 "결과는 다음과 같습니다" 등의 불필요한 서술형 인사말이나 사족은 절대 포함하지 말고, 곧바로 (혹은 태그 출력 후) 1번 항목부터 순수하게 마크다운 형식의 결과만 출력해.
주의사항 2: 원문 텍스트 내에서 (보통 맨 앞부분, 제목 주변 혹은 머리말 등에) "PRACTICE BRIEF"라는 문구가 명시되어 있는 논문의 경우, 분석 결과 맨 첫 줄(1번 항목 시작하기 바로 위)에 반드시 `**[TAG: PRACTICE BRIEF]**` 라고 굵은 글씨로 태그를 달아줘. 일반 정규 논문이면 아무 태그도 달지 마.
주의사항 3: **반드시** 모든 항목의 말머리는 `숫자. **항목명**:` 포맷으로 일관되게 작성해. `###` 기호 등은 절대 쓰지 마. (예시: `1. **연구 주제**: 이 논문은...`)

1. **연구 주제**: (이 논문이 다루는 핵심 문제와 목적을 간결하게 설명)
2. **연구 참여자**: (표본 크기, 인구통계학적 특성, 배경 등을 간략히 명시)
3. **이론적 틀**: (이 연구를 지탱하는 핵심 이론적 배경이나 렌즈가 무엇이며 어떻게 적용되었는지 핵심만 서술)
4. **연구 방법론**: 각 논문을 (A) 연구 접근 유형(질적 연구, 양적 연구, 혼합방법 연구, 문헌분석/리뷰, 그 외(예: 개념·이론 논문, 방법론/측정도구 개발, 에세이/논설, 실무 브리프 등))으로 분류하고, (B) 원문에 구체적 연구 방법론(디자인/기법)이 쓰여 있으면 그 이름을 함께 간략히 적어줘. 세부적인 방법 명시가 전혀 없으면 세부 방법은 "미상/명시 없음"으로 둠.
5. **데이터 수집 방법**: Sampling method가 쓰여 있으면 표집 방법을 적어주고 없으면 "명시 없음"으로 둠. 데이터 수집 절차(설문조사, 인터뷰, 관찰 등)를 간결하게 서술해줘.
6. **데이터 분석 방법**: (수집된 데이터가 어떤 분석 기법, 소프트웨어 혹은 코딩 접근법을 거쳐 해석되었는지 간략히 설명)
7. **논문 초록에서 언급된 키워드 추출**: (반드시 각 키워드마다 `- 키워드` 형태의 글머리 기호(bullet point)를 사용하여 통일성 있게 작성하고, 한글 번역 없이 원문(영어) 키워드만 추출해)
8. **게재 연도**: (순수하게 숫자만 출력해. 예: 2022)
"""

VALIDATION_PROMPT = """
다음은 논문의 원문 텍스트입니다:

{paper_text}

---
다음은 위 논문에 대한 분석 결과 텍스트입니다:

{analysis_text}

---
위 분석 결과 텍스트가 다음 10가지 검증 기준을 완벽하게 통과하는지 평가해 주세요:
1. 필수 항목 완전성 (Completeness): "1. **연구 주제**:"부터 "8. **게재 연도**:"까지 8개의 항목이 모두 존재하는가?
2. 사족 제거 (No Conversational Filler): 처음이나 끝에 "다음은 결과입니다", "분석을 마쳤습니다"와 같은 대화형 문장이 없는가?
3. 마크다운 포맷팅 통일 (Consistent Formatting): 모든 1~8번 항목이 정확히 `숫자. **항목명**:` 형태로 시작되는가? (예: `1. **연구 주제**:`, `3. **이론적 틀**:`. 만약 `### 1.` 나 `1. 연구 주제:` 처럼 볼드가 빠지거나 다른 형식으로 시작한다면 실패 처리할 것)
4. 내용의 정확성 (Content Correctness): 각 항목의 내용이 분석 지시에서 요구하는 바를 정확하게 반영하고 있는가?
5. 연구 방법론 지시사항 준수: "4. **연구 방법론**:" 항목에 (A) 연구 접근 유형과 (B) 구체적 연구 방법(또는 "미상/명시 없음")이 모두 잘 구분되어 작성되었는가?
6. 표집 방법 지시사항 준수: "5. **데이터 수집 방법**:" 항목에 표집 방법(Sampling method) 내용(또는 "명시 없음")이 명확히 포함되어 있는가?
7. 키워드 포맷 (Bullet Points): "7. **논문 초록에서 언급된 키워드 추출**:" 항목의 내용이 쉼표 나열이 아닌 `- 키워드` 형태의 글머리 기호 나열 방식으로 통일되어 있는가?
8. 키워드 언어 (English Only): 7번 항목(키워드)에 (한글 번역)이 포함되지 않고 오직 원문(영어) 형태로만 작성되었는가?
9. 연도 포맷 (Year Format): "8. **게재 연도**:" 항목이 서술형이 아닌 명확한 연도 숫자(예: 2022) 형태로 작성되었는가?
10. 원문과의 연관성 (Groundedness / Hallucination Check): 분석된 내용이 PDF 원문에 실제로 등장하는 내용인가? 모델이 그럴싸하게 지어낸(Hallucinate) 내용이 포함되어 있지 않은가?

평가 결과는 반드시 다음과 같은 엄격한 JSON 형식으로만 반환하세요 (다른 말은 절대 추가하지 마세요):
```json
{{
  "is_valid": true 혹은 false,
  "reason": "만약 false라면, 어떤 기준을 통과하지 못했는지 구체적인 이유. true라면 빈 문자열"
}}
```
"""
